# --- SlotScribe 生产环境配置指南 ---

# 1. 存储配置 (Cloudflare R2)
# 推荐权限: Object Read & Write 或 Admin Read & Write
R2_ACCESS_KEY_ID=你的_R2_ACCESS_KEY_ID
R2_SECRET_ACCESS_KEY=你的_R2_SECRET_ACCESS_KEY
R2_ENDPOINT=https://<ACCOUNT_ID>.r2.cloudflarestorage.com
R2_BUCKET_NAME=slotscribe

# 2. 区块链节点配置 (推荐使用私有 RPC 如 Helius/QuickNode)
# 如果不填写，将回退使用 Solana 公共节点 (有较严频率限制)
MAINNET_RPC_URL=https://mainnet.helius-rpc.com/?api-key=你的KEY
DEVNET_RPC_URL=https://devnet.helius-rpc.com/?api-key=你的KEY

# 3. 全局默认网络开关
# 可选值: mainnet-beta, devnet (开发测试时建议设为 devnet)
NEXT_PUBLIC_DEFAULT_CLUSTER=mainnet-beta

# 4. 其他配置
TRACES_DIR=./data/traces
